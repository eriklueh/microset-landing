[build]
  command = "pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback: serve index.html for any path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
